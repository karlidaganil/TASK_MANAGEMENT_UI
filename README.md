Frontend Setup & Run Instructions

After cloning the project, install the required packages using either of the following commands:

yarn


or

npm install

Starting the Project

Run the project with:

yarn start


The application will start on the following local development server:

http://localhost:5173

Once the frontend is running, it will communicate with the Web API provided by the backend.
